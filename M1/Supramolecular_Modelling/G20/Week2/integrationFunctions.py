import numpy as np
import matplotlib as plt

# net area under the curve over interval [a,b]

# the points
# (x(i-1), f(x(i-1))) , (x(i), f(x(i)))

# (x(i-1), 0), (x(i), 0)

# trapezoid area = (0.5)*(f(x(i))+f(x(i-1))*(x(i)-x(i-1)))



